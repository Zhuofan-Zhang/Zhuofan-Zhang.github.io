export enum SelectedPage {
  Home = "home",
  Experiences = "experiences",
  Portfolios = "portfolios",
  Blogs = "blogs",
  Contacts = "contacts"
}

export interface ClassType {
  name: string;
  description?: string;
  image: string;
}
